<div class="wrapper">
  <div class = "card card-body">
    <h2>Quality:</h2>
    <% field.values.each do |value| %>
      <input type="checkbox" name="call[details[chest_pain_quality]][]" value="<%= value %>">
      <label><%= value %></label>
    <% end %>
  </div>
</div>
