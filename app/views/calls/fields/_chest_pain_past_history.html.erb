<div>
  <h2 class="btn btn-primary" data-toggle="collapse" href="#chest_pain_past_history" role="button" aria-controls="collapse">Past History:</h2>
  <div class="collapse" id="chest_pain_past_history">
    <% field.values.each do |value| %>
      <input type="checkbox" name="call[details[chest_pain_past_history]][]" value="<%= value %>">
      <label><%= value %></label>
    <% end %>
  </div>
</div>
